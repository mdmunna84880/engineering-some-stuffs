const stack = document.getElementsByClassName("stack");

// stack[0].children.forEach(element => {
//     console.log(element.innerHTML);
// });
console.log(stack[0])
for(let i = 0; i < stack[0].children.length; i++){
    console.log(stack[0].children[i]);
}